on:
  push
jobs:
  ubuntu20-depends:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v3

      - name: Update apt repos
        run: sudo add-apt-repository universe && sudo apt-get update
             
      - name: Install base dependencies      
        run: sudo apt-get install build-essential xutils-dev libtool gperf autotools-dev automake pkg-config bsdmainutils libattr1-dev make automake bison byacc cmake curl g++-multilib binutils-gold bison byacc python3 libevent-dev libboost-all-dev qtbase5-dev qttools5-dev-tools

      - name: Install dependencies for building QtWebKit
        run: sudo apt-get install libx11-dev libjpeg-dev libpng-dev libtiff-dev libwebp-dev libxslt-dev qtdeclarative5-dev qtpositioning5-dev
      
    #  - name: Clone and build QtWebKit from source
    #    run: |
    #      git clone https://github.com/qt/qtwebkit.git
    #      cd qtwebkit
    #      git checkout 5.212  # Checkout the correct branch for Qt5
    #      mkdir build
    #      cd build
    #      cmake ..
    #      make -j$
    #      sudo make install

      - name: Clone and build QtSensors from source
        run: |
          git clone https://github.com/qt/qtsensors.git
          cd qtsensors
          mkdir build
          cd build
          cmake ..
          make -j$(nproc)
          sudo make install
      
      - name: List current directory
        run: ls -R

      - name: execute permission for autogen.sh
        run: chmod +x autogen.sh

      - name: run autogen.sh to generate configure script
        run: ./autogen.sh
            
      - name: execute permission to configure script
        run: chmod +x configure

      - name: configure
        run: CONFIG_SITE=$PWD/depends/x86_64-linux-gnu/share/config.site ./configure --enable-gui --disable-bench --disable-tests --disable-dependency-tracking --disable-werror --prefix=`pwd`/depends/x86_64-linux-gnu

      - name: Build project
        run: make -j$
